org.gradle.jvmargs=-Xmx2g
kotlin.stdlib.default.dependency=false
# Android Studio's bundled JBR satisfies the platform's JDK 21 requirement; Gradle
# doesn't scan inside app bundles by default, so point it there explicitly.
org.gradle.java.installations.paths=/Applications/Android Studio.app/Contents/jbr/Contents/Home
